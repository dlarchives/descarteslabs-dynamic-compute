[tool.poetry]
name = "descarteslabs-dynamic-compute"
version = "1.6.0post0"
description = "Discontinued. Please use earthdaily-earthone-dynamic-compute instead."
license = "Apache-2.0"
authors = ["EarthDaily"]
readme = "README.md"
packages = [
    { include = "descarteslabs/dynamic-compute" },
]

[tool.poetry.dependencies]
python = ">=3.10, <3.13"